import { createTheme } from '@mui/material/styles';

// Define your custom theme
export const theme = createTheme({
  palette: {
    primary: {
      main: '#1976D2', // Change this to your primary color
    },
    secondary: {
      main: '#E91E63', // Change this to your secondary color
    },
  },
});
